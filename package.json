{
  "name": "linepayusdt",
  "version": "1.0.0",
  "description": "Kaia-native USDT payments for LINE Mini DApp",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd contracts && npm install && cd ../frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev:contracts\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:contracts": "cd contracts && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "cd contracts && npm test",
    "deploy:testnet": "cd contracts && npm run deploy:testnet",
    "deploy:mainnet": "cd contracts && npm run deploy:mainnet"
  },
  "keywords": [
    "kaia",
    "usdt",
    "line",
    "mini-dapp",
    "payments",
    "blockchain",
    "defi"
  ],
  "author": "LINEPayUSDT Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "workspaces": [
    "contracts",
    "frontend",
    "backend"
  ]
}